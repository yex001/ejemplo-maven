pipeline {
  agent none
  stages { 
    stage("Check Maven in PATH") {
      steps {
        script {
          sh 'echo $PATH'
        }
      }
    }
    stage("build & sonarqube") {
      agent any
        steps {
          withSonarQubeEnv('SonarQube_server') {
            sh 'mvn clean package sonar:sonar'
          }
        }
    }
  }
}
